# macOS
.DS_Store

# Obsidian workspace (local only)
.obsidian/workspace
.obsidian/workspace.json

# Search index build artifact (rebuilt locally)
Intelligence/_search/index.db

# OKF export bundles (regenerated by the export verb)
Intelligence/_export/out/
Intelligence/_export/*.tar.gz
