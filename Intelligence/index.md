# Intelligence — wiki index

The wiki has two top-level groupings: **bundles** (user-curated) and
the **`_unsorted/`** quarantine. The agent never invents a bundle;
new bundles are a human decision.

## Bundles

- [[domain-a/index|Domain A]] — placeholder bundle demonstrating multi-topic linking and image co-location.
- [[domain-b/index|Domain B]] — placeholder bundle demonstrating a single-topic case.

## Quarantine

- [[_unsorted/index|_unsorted]] — articles that matched no bundle. Human review required.
