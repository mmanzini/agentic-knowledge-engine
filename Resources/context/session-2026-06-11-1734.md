---
captured: 2026-06-11T15:34:39Z
origin: session-end-hook
---

# Session signals — 2026-06-11-1734

- **STORE / INJECT / RECALL as explanation model** — User preferred this framing after seeing it in a video; drove restructuring of all diagrams and chapters. Clear principle for explaining context flow.

- **Neutral/shareable documentation** — User wants examples generic enough to distribute widely; stripped personal details (framework name, user identifiers) from worked example to maximize reusability.

- **Token accounting reveals trade-offs** — Labeling token costs on diagram blocks surfaced design insight: routing indexes cost ~1/10 of article bodies, justifying tight body budgets.

- **Atlas 3-tier memory architecture** — Tier 0 = snapshot.md (~0.7k, SessionStart-injected); Tier 1 = index walk; Tier 2 = _search/ (SQLite FTS5 + embeddings, pointers-only fallback). SessionEnd hook captures sessions. Auto-chain rebuilds search index post-refine.

- **Excalidraw plugin merge gotcha** — Plugin auto-saves cached scene back to disk, overwriting external writes if tab open. Close tab before programmatic regeneration or split diagrams across files to avoid merge.
